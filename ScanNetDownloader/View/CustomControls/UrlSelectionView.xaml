<UserControl x:Class="ScanNetDownloader.View.CustomControls.UrlSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanNetDownloader.View.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid Grid.Row="1" Visibility="Visible">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.63*"/>
            <RowDefinition Height="0.37*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TextBlock Name="txtBlockInfos"  Text="{Binding SelectUrlInfos}" VerticalAlignment="Center" Margin="30" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="1" Margin="10 0 10 0">
            <Label Content="Enter the URL here"/>
            <TextBox Name="txtBoxUrlInput" Text="{Binding UrlInput, UpdateSourceTrigger=PropertyChanged}" TextChanged="txtBoxUrlInput_TextChanged" VerticalContentAlignment="Center" Height="30" Background="Beige"/>
            <Label Name="lbUrlError" Content="{Binding ErrorMessage}" Foreground="Red"/>
        </StackPanel>
        <Button Grid.Row="2" Name="btnNext" Content="Next" Click="btnNext_Click"/>
    </Grid>
</UserControl>
