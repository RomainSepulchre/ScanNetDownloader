<UserControl x:Class="ScanNetDownloader.View.CustomControls.ChaptersSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanNetDownloader.View.CustomControls"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <CollectionViewSource x:Key="ChapterItemsSource" Source="{Binding ChapterItems}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="SortingId"/>
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="20 13 20 0">
            <TextBlock Text="{Binding SelectChapterInfos, FallbackValue=Select chapters for...}" FontSize="24" FontWeight="Bold" TextWrapping="NoWrap" Margin="0 0 0 5"/>
            <!--<TextBlock Name="txtBlockSelectChapterInfo" Text="{Binding SelectChapterInfos}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30 0 30 0" TextWrapping="Wrap"/>-->
            <TextBlock Text="Add a single chapter" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Name="txtBoxSingleChapter" Text="{Binding SingleChapterInput}" TextChanged="txtBoxSingleChapter_TextChanged" Height="30" Width="172" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="{StaticResource Colors.White}" BorderBrush="{StaticResource Colors.DarkGrey}"/>
                <Button Name="btnAddSingleChapter" Content="Add single chapter" Click="btnAddSingleChapter_Click" Height="30" Width="178" Margin="20 0 0 0" FontSize="16"/>
            </StackPanel>
            <local:ErrorAlert x:Name="errorAlertAddSingle" ErrorMsg="{Binding ErrorMessageSingle}" Height="10" Visibility="Hidden"/>
            <TextBlock Text="Add a range of chapter" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Name="txtBoxStartChapter" Text="{Binding StartChapterInput}" TextChanged="txtBoxStartChapter_TextChanged" Height="30"  Width="172" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="{StaticResource Colors.White}" BorderBrush="{StaticResource Colors.DarkGrey}"/>
                <TextBlock Text="-" TextAlignment="Center" VerticalAlignment="Center" Width="20"/>
                <TextBox Name="txtBoxEndChapter" Text="{Binding EndChapterInput}" TextChanged="txtBoxEndChapter_TextChanged" Height="30" Width="172" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Background="{StaticResource Colors.White}" BorderBrush="{StaticResource Colors.DarkGrey}"/>
                <Button Name="btnAddRangeOfChapter" Content="Add range of chapter" Click="btnAddRangeOfChapter_Click" Width="178" Margin="20 0 0 0" FontSize="16"/>
            </StackPanel>
            <local:ErrorAlert x:Name="errorAlertAddRange" ErrorMsg="{Binding ErrorMessageRange}" Height="10" Visibility="Hidden" />
            <!--<TextBlock Name="txtBlockChapterError" Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>-->
        </StackPanel>


        <Border Grid.Row="1" Margin="20 10 20 20" Background="{StaticResource Colors.LightGrey}" BorderThickness="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl Name="chapterSelectedPanel" ItemsSource="{Binding Source={StaticResource ChapterItemsSource}}" VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" IsItemsHost="True" HorizontalAlignment="Center" VerticalAlignment="Center">
                            </WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!--<StackPanel Grid.Row="2">
            <Label Content="Chapters selected" FontWeight="Bold"/>
            <ItemsControl Name="chapterSelectedPanel" ItemsSource="{Binding Source={StaticResource ChapterItemsSource}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True" HorizontalAlignment="Center" VerticalAlignment="Center">
                        </WrapPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>-->

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.25*"/> 
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="0.75*"/>
            </Grid.ColumnDefinitions>

            <Button Name="btnBackToUrlSelection" Content="Back" Click="btnBackToUrlSelection_Click" FontSize="22" FontWeight="Normal"/>
            <Rectangle Grid.Column="1" Fill="{StaticResource Colors.White}"/>
            <Button Grid.Column="2" Name="btnConfirmChapters" Content="Next" Click="btnConfirmChapters_Click" FontSize="22" IsEnabled="False"/>
        </Grid>
    </Grid>
</UserControl>
