<UserControl x:Class="ScanNetDownloader.View.CustomControls.ScanDataCreationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanNetDownloader.View.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <StackPanel Margin="20 13 20 10">
            <TextBlock Text="Retrieve scan data" FontWeight="Bold" FontSize="24" Margin="0 0 0 10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Progress..." VerticalAlignment="Center" Margin="0 0 13 0" FontSize="14"/>
                <ProgressBar Grid.Column="1" Name="progrBarScanDataCreation" Minimum="0" Maximum="100" Height="25" Style="{DynamicResource DefaultProgressBarStyle}"/>
                <TextBlock Grid.Column="1" Text="{Binding ProgressStatus}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="20 0 20 20" VerticalScrollBarVisibility="Auto">
            <StackPanel Background="{StaticResource Colors.LightGrey}" Name="listVwCreationStatus">
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="3" Name="btnFinish" Content="Finish" Click="btnFinish_Click" FontSize="22" IsEnabled="False"/>
    </Grid>
</UserControl>
