<UserControl x:Class="ScanNetDownloader.View.CustomControls.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanNetDownloader.View.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <UserControl.Resources>
        <CollectionViewSource x:Key="DownloadItemsSource" Source="{Binding DownloadItems}">
        </CollectionViewSource>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Grid Name="gridDlInfo">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                <ItemsControl Name="stPanelDownloadInfo" ItemsSource="{Binding Source={StaticResource DownloadItemsSource}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel IsItemsHost="True" Background="LightGray" Margin="5">
                            </StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <TextBlock Name="txtBlockNoScanSelected" Text="No scans have been selected for download, select at least a scan to start the download" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.9*"/>
            </Grid.ColumnDefinitions>

            <Label Content="{Binding DownloadLabelTxt}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <ProgressBar Grid.Column="1" Name="progrBarDownload" Minimum="0" Maximum="100" Value="{Binding DownloadProgress}" Margin="0 10 10 10"/>
        </Grid>

    </Grid>
</UserControl>
