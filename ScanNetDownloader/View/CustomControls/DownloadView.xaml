<UserControl x:Class="ScanNetDownloader.View.CustomControls.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScanNetDownloader.View.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="660" d:DesignWidth="1280">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../MainResourcesDictionnary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <CollectionViewSource x:Key="DownloadItemsSource" Source="{Binding DownloadItems}">
            </CollectionViewSource>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="20 8 20 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid Name="gridDlInfo" Margin="0 0 0 20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
                <ItemsControl Name="stPanelDownloadInfo" ItemsSource="{Binding Source={StaticResource DownloadItemsSource}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel IsItemsHost="True" Background="{StaticResource Colors.LightGrey}">
                            </StackPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <TextBlock Name="txtBlockNoScanSelected" Text="No scans have been selected for download, select at least a scan to start the download" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="235"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Button Name="btnStartDl" Background="Black" Foreground="White" >
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Margin="-2 0 0 0">
                        <Image Source="{StaticResource Img.startIconWhite}" Height="40" Width="40" VerticalAlignment="Center" Margin="0 0 2 0"/>
                        <TextBlock Text="Start Download" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <Button Name="btnStopDl" BorderThickness="0" Background="Black" Foreground="White" Visibility="Collapsed">
                <Button.Content>
                    <StackPanel Orientation="Horizontal" Margin="-2 0 0 0">
                        <Image Source="{StaticResource Img.stopIcon}"/>
                        <TextBlock Text="Stop download" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0 0 2 0"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            
            
            <ProgressBar Grid.Column="1" Name="progrBarDownload" Minimum="0" Maximum="100" Value="{Binding DownloadProgress}" Margin="10 0 0 0" BorderThickness="0" Background="{StaticResource Colors.LightGrey}" Foreground="{StaticResource Colors.Green}"/>
            <Label Grid.Column="1" Content="{Binding DownloadLabelTxt}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
 
        </Grid>

    </Grid>
</UserControl>
